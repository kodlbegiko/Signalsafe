# Known limitations

- Local persistence currently uses browser localStorage rather than IndexedDB.
- The question bank is synthetic and still requires expert review and A/B difficulty calibration.
- No education-effectiveness claim has been tested.
- Offline support requires a first successful HTTP load and a browser with service worker support.
- Import replaces the current local data after user confirmation.
